import { Pin } from "@devicescript/srvcfg"
import {
    ArchConfig,
    DeviceConfig,
} from "../devicescript/interop/src/archconfig"

interface STM32DeviceConfig extends DeviceConfig {}

interface STM32ArchConfig extends ArchConfig {}
